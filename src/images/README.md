Place the provided image file here and name it `tycoon-stats.png`.

Recommended steps:

1. Save the attachment image from the chat as `tycoon-stats.png`.
2. Put it in `src/images/` (project-relative path).
3. Open `index.html` in a browser to verify it appears in the "Tycoon RNG" gallery.

If you want me to add the actual image file, upload it into the workspace or tell me and I'll add it for you.